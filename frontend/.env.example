# Vapi public (publishable) key -- safe to ship to the browser, like a Stripe
# publishable key. Get it from the Vapi dashboard.
VITE_VAPI_PUBLIC_KEY=

# Backend base URL. Defaults to http://localhost:8000 if unset.
VITE_API_BASE_URL=http://localhost:8000
